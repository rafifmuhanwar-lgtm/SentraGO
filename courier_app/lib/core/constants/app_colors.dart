import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary (Burgundy / Maroon) ──
  static const Color primary = Color(0xFF7F1D3A);
  static const Color primaryDark = Color(0xFF5A1228);
  static const Color primaryLight = Color(0xFFA83250);

  // ── Semantic ──
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFE65100);
  static const Color error = Color(0xFFC62828);

  // ── Surfaces ──
  static const Color background = Color(0xFFF8F6F2);
  static const Color surface = Colors.white;
  static const Color border = Color(0xFFE0DDD7);
  static const Color divider = Color(0xFFEFEDE8);

  // ── Text ──
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF777570);
  static const Color textLight = Colors.white;
}
