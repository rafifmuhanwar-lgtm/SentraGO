import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF7F1D3A); // Burgundy
  static const Color primaryLight = Color(0xFF9E2C4C);
  static const Color primaryDark = Color(0xFF5D1227);

  // Background
  static const Color background = Color(0xFFFAF8F8); // Light Background
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Colors.white;

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFB300);

  // Borders & Dividers
  static const Color divider = Color(0xFFEEEEEE);
  static const Color border = Color(0xFFE0E0E0);
}
